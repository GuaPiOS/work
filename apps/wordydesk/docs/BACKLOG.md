# WordyDesk Backlog

## P0

- 验证 `Services` 入口在常见应用中的实际可见性和稳定性
- 验证拖动浮窗时自动隐藏逻辑是否仍然符合预期
- 验证云 TTS 在真实 API key 下的延迟、失败回退和音质

## P1

- 把 `AppModel.swift` 拆成更小的协调器对象
- 把 `ContentView.swift` 拆成 `Now/Review/Plan` 子视图
- 把例句生成从模板句升级为真实英语学习数据源
- 为右键服务增加更明确的系统注册说明

## P2

- 增加词条编辑能力
- 增加更细的复习热力图和日历视图
- 增加导出/导入学习数据

## 当前不建议立即做

- 先不要继续堆更多入口形态
  - 当前已有自动选词、快捷键、Services、菜单栏、独立窗口
  - 入口已经够多，先把稳定性打牢

- 先不要继续把 README 做成总状态板
  - 它会再次污染上下文
  - 状态应集中在 `apps/wordydesk/docs/`
