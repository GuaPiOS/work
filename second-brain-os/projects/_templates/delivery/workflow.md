# Delivery Workflow

这是一个 `delivery` 类型项目。

## 关注重点

- 交付结果和截止时间最重要
- `brief.md` 和 `scope.md` 必须在早期稳定
- `artifacts/current/` 是主结果区

## 默认顺序

1. 定义交付物和成功标准
2. 明确边界和依赖
3. 规划里程碑
4. 逐版输出 artifact
5. 收尾归档
